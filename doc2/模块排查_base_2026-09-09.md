# 模块排查报告：base（基座层）

日期：2026-09-09 · 模块：libop/base · 范围：12 文件 / 1214 行（全部通读 + 交叉引用核查）
结论：**质量总体高**——KMP/replace/Delay/Delays/Json 转义/SAFE 宏均带历史修复注释（被反复打磨过）。发现 1 必修 + 3 建议修 + 6 可选，另澄清 3 处疑似非问题。

## 模块档案

| 项 | 值 |
|---|---|
| 文件 | AutomationModes.h/.cpp、Environment.h/.cpp、JsonUtils.h、ThreadPool.h、Types.h、Utils.h/.cpp、WindowsHandle.h、WindowsVersion.h/.cpp |
| 依赖 | 无内部模块依赖（纯工具）；**被全库 17 模块依赖**（根节点） |
| 使用者 | 日志 setlog ~190 处（宽字符版 58 处）、Delay/Delays 41 处、ThreadPool 2 处（TemplateMatcher 静态池 + ImageSearch 每实例池）、hex2bin 2 处（ProcessMemory/Dictionary） |

## 一、必修（崩溃/UB）

### B1 `setlog(wchar)` 二次格式化 UB —— Utils.cpp:106
- **现象**：宽字符版把**已格式化完成的文本**当作 format 再传给窄字符版：
  ```cpp
  long setlog(const wchar_t *format, ...) {
      ... vswprintf_s(...) // 得到 tmps
      return setlog(tmps.data()); // ← 已展开文本当 format，char 版内部再 va_start/vsprintf_s
  }
  ```
  char 版被调用时**实参只有 1 个（无 vararg）**，却执行 `va_start + _vscprintf + vsprintf_s`——读取不存在的可变参数属 UB。
- **触发**：格式化结果含 `%` 字面（如 `setlog(L"进度 %d%%", x)` 展开出单个 `%`，或 `%s` 展开的内容本身含百分号）。含 `%s/%d` 占位时 `vsprintf_s` 解引用垃圾指针 → **潜在崩溃/内存破坏**。
- **触发面**：全项目宽字符版调用 58 处（HookCapture 20 / BindingSession 12 / InputHookClient 12 / ICaptureBackend 4 / WgcCapture 3 / DllInjector 3 / 其余 4 文件 4 处）。多数展开无 `%` 而侥幸安全——隐患在含百分号的文本。
- **修复方案（~20 行，低风险）**：把窄字符版的"输出体"抽为内部 `setlog_text(const std::string &s)`（时间戳+级别+分发，不碰 va_list）；窄字符版 = 格式化 → `setlog_text`；宽字符版 = 转码 → `setlog_text`。消除二次解析。

## 二、建议修（边界可能错）

### S1 `rect_t::divideBlock` count<=0 除零 + count>span 产生 0 尺寸块 —— Types.h:96-119
- `NumberGen(height(), count)` → `_q = n / cnt`，count=0 时 **release 构建除零崩溃**（`assert(count>0)` 仅 debug 生效）；count>height/width 时尾部产生 **0 尺寸块**（分块结果含空 rect，调用方对 0 块处理未知）。
- 调用方：ImageSearchAlgorithms.cpp:658/773，count 源自用户可配置分块数。image 模块批 2 细查时确认取值域。
- 修复：入口防御 `if (count <= 0) return;`（或 clamp 到 [1, span]）。

### S2 `hex2bin` 不认小写 a-f、无范围校验 —— Utils.h:32
- `c <= L'9' ? c - L'0' : c - L'A' + 10`：输入 `'a'` 返回 -22、`'g'` 返回垃圾。调用方 ProcessMemory.cpp:597 先 `towupper`（安全），**:552 直调未转大写**——用户在小写 hex（记忆搜索/找数据特征码）时解析错乱。
- 修复：hex2bin 内 `c = std::toupper(c)` 再判，或 552 行补 towupper（后者属 memory 模块，批 1 顺手）。

### S3 `ThreadPool` 构造 0 线程无防护 —— ThreadPool.h:38
- `ThreadPool(0)` → workers 空，enqueue 的 future **永不 ready**（调用方 wait 死等）。
- 现状两调用方均已防护（TemplateMatcher `max(1u, hw)`、ImageSearch `hw==0?1:hw`）——**当前不触发**，纯防御加固。修复一行：`threads = threads ? threads : 1`。

## 三、可选（理论可改，当前不触发）

| # | 位置 | 内容 |
|---|---|---|
| O1 | Utils.cpp:129 | setlog 输出恒定 `"info: "`，无 error/warn 级别；与 `m_showErrorMsg==1` 弹 MessageBox 的 error 语义不一致 |
| O2 | Utils.cpp:135-146 | `m_showErrorMsg` 非原子 int，HookExport(注入进程置 2)/OpRuntime(SetShowErrorMsg) 与各线程 setlog 读构成 data race（x86 实际安全）；`__op.log` 追加无跨线程/跨进程锁 |
| O3 | Utils.cpp:311 | `Delays` 用 `rand()`：非线程安全 + 质量低，建议 `rand_s` 或 thread_local mt19937 |
| O4 | Utils.cpp:254 | `GetLastErrorAsString`：FormatMessageA 失败时 `std::string(messageBuffer, 0)` 传 nullptr（MSVC 实际安全，严格 UB），可加 `size==0` 早退 |
| O5 | Environment.h | RuntimeEnvironment 静态量（m_instance/m_basePath/m_opName）启动期一次性设置，多线程并发 setInstance 无保护（实际不会） |
| O6 | AutomationModes.h:123 | OP_VERSION 0.4.8.3 宏硬编码，与 CMake/rc 版本单点，升版漏改风险 |
| O7 | AutomationModes.h:87-101 | MakeOpSharedResourceName/MakeOpMutexName 名值颠倒（注释已警示，改名需宿主+注入 DLL 同步重编）——**维持不治** |

## 四、已澄清非问题（疑似驳回）

1. **JsonUtils UnescapeString emoji 代理对**（JsonUtils.h:146-155）：初判索引错位，复核确认 `ParseHex4` 后有 `i += 4`，`value[i+1]/[i+2]` 恰指低代理 `\u`——**逻辑正确**，撤销。
2. **KEYPAD\*/MOUSE\* 延迟 extern**：全部被键盘/鼠标模块使用（WinKeyboard.cpp:213-228、DxKeyboard.cpp:93、WinMouse.cpp 多处），非死代码。
3. **point_t::row_height / ThreadPool**：row_height 全项目**无运行期写点**（无 data race）；ThreadPool 两调用方已防 0（见 S3）。

## 五、拍板建议

| 项 | 风险 | 改动量 | 建议 |
|---|---|---|---|
| B1 setlog 二次格式化 | 崩溃/UB（触发面 58 处宽版调用） | ~20 行重构 | **修**（低风险高收益） |
| S1 divideBlock 除零 | release 除零（需 image 确认取值域） | ~3 行 | 修（随 image 批或立即可） |
| S2 hex2bin 小写 | 小写特征码解析错乱 | 2 行 | 修（ProcessMemory:552 属批1 memory 顺手） |
| S3 ThreadPool 防 0 | 当前不触发 | 1 行 | 可修（零成本） |
| O1-O7 | 低 | — | 报告留档，不强制 |

## 六、执行记录（2026-09-09）

按拍板建议执行 **B1+S1+S2+S3 全部修复**，编译验证通过：

| 项 | 改动 | 文件 |
|---|---|---|
| B1 | 抽 `setlog_text()` 内部输出体，宽/窄两版均"格式化一次→落盘"，消除二次 va_start UB | Utils.cpp |
| S1 | `divideBlock` 入口 `count<=0` 早退（clear）+ `count>span` clamp 到 span，防除零与 0 尺寸块 | Types.h |
| S2 | `hex2bin` 入口归一化小写 a-f（改 base 本体，全局通吃，不再依赖调用方 towupper） | Utils.h |
| S3 | `ThreadPool` 构造 `threads==0 → 1`，防 enqueue future 永不 ready | ThreadPool.h |

验证：nmake 增量全编 8m52s **EXIT=0 / 0 error**（Types.h/Utils.h 触发全库重编）；op_test 回归 **182 用例 / 146 PASS / 1 已知 MouseKey 环境 FAIL / 2 DISABLED = 零回归**。产物已同步 bin/x64。
