# OP 插件第二轮全盘核查报告

日期：2026-09-08 ｜ 基线：70170dc（YOLO 内置化提交后）
方式：4 路子代理分模块只读深扫 + 关键项人工复核（行号级证据）
范围：libop 全部 20 个子目录（149 源文件），重点覆盖金字塔评审未触及的 window/binding/ipc/network/input/capture/hook/opencv

---

## 一、BUG / 风险清单（按分级）

### P0 必修（崩溃/越界写/野指针/功能确定错误）

| # | 位置 | 问题 | 影响 | 复核 |
|---|---|---|---|---|
| 1 | `capture/backends/GdiCapture.cpp:288-300` | PrintWindow 分支每帧按**当前整窗** ww×wh 写共享段，而 `_shmem` 按**绑定时刻**客户区尺寸分配 → 窗口 resize 放大即越界写击穿共享内存；同帧 `fmtFrameInfo(pshare,_hwnd,w,h)` 头部写请求区尺寸、载荷却是整窗尺寸（头/载荷不一致）；`_pmutex` 被注释（L287） | GDI 是默认回退后端（BindingSession:217），resize 场景数据损坏/崩溃，多线程读半写头 | ✅ 人工复核 |
| 2 | `hook/D3D12Capture.cpp:167-172` | `commandQueueOffset_` 恒为 0 从未赋值 → `*(uintptr_t*)(swapchain对象地址+0)` 把 **COM vtable 指针当 ID3D12CommandQueue\*** 调 `ExecuteCommandLists`；连带 commandAllocator 每帧不 Reset（首帧后 CreateCommandList 失败）、readback 无 fence 同步 | D3D12 渲染游戏每帧野指针调用，必然崩溃/UB（该路径经 DisplayHook.cpp:85 detour 可达） | ✅ 人工复核 |
| 3 | `image/ImageSearchService.cpp:906` | `str2colordfs` 内层 `split(it,L"-")` 空段时 `vstr2[0]` 空 vector 取下标（UB）——颜色串含前导/连续 `|` 即触发 | CmpColor/FindColor/FindMultiColor/Ocr 共用解析器，坏参即崩 | ✅ 人工复核 |
| 4 | `window/WindowService.cpp:1771、1814` | `titles = titles;` **自赋值**（应为 `titles = title;`），复制粘贴错 | FindWindowByProcess(进程+标题) 的子窗口递归永久丢失标题过滤，只按类名匹配 | ✅ 人工复核 |
| 5 | `input/mouse/WinMouse.cpp:228-238`、`DxMouse.cpp:47-57` | `POINT pt` 未初始化；`GetCursorPos`/`ScreenToClient` 失败（锁屏/会话切换）后仍 `x=pt.x;y=pt.y` 输出垃圾坐标，且返回 0 无法区分 | 读未初始化栈值 | ✅ 人工复核 |

### P1 建议修（输入健壮性/竞态可疑/边界错）

| # | 位置 | 问题 | 说明 |
|---|---|---|---|
| 6 | `window/WindowService.cpp:1713-1716、1755-1756、1800-1801` | FindWindow/FindWindowEx 对 NULL class_name/title 直接 `[0]` 解引用；FindWindowByProcess 在判空**之前**执行 `wcsstr(str, class_name)`（class_name 可能 NULL） | COM 层 BSTR 直传无空转义（OpAutomation.cpp:199-201），C++ 调用方传 NULL 即崩 |
| 7 | `window/WindowService.cpp:135-160` | GetWindowState flag==1（激活）用 `GetActiveWindow()`——只查**调用线程**活动窗口，跨进程目标恒 false；flag==5（置顶）用 `GetForegroundWindow()` 判定不严谨（WS_EX_TOPMOST 才是置顶） | 语义偏离大漠标准 |
| 8 | `hook/InputHook.cpp:974-1005、1035-1084` | `m_mouseState.lAxisX+=dx` / `m_wheelDelta` / `m_vkState` 写侧（窗口线程）无锁，读侧（游戏轮询线程 `GetDeviceState`/`GetAsyncKeyState`）同样无锁 read-modify-write | 游戏多线程轮询时位移/滚轮重复累加、状态撕裂 |
| 9 | `hook/InputHook.cpp:874-888` | `hook_dinput()` 失败仅 setlog 继续装其余 hook，`setup()` 无条件 return 1 → 宿主标记绑定成功，DirectInput 通道静默全失效 | 失败谎报成功 |
| 10 | `hook/InputHookClient.cpp:216-220` | `g_bind_refs` 先 erase 再调远端 `ReleaseInputHook` 且不校验返回值 → 远端释放失败时注入 DLL 残留、宿主状态丢失不可恢复 | 资源泄漏+状态不可恢复 |
| 11 | `ipc/Pipe.cpp:116-127` | reader 线程阻塞 ReadFile，`close()` 置标志无法中断；子进程派生的孙进程继承写端句柄存活时永不 BROKEN_PIPE → `join()` 无限挂起 | GetCmdStr 超时失效 |
| 12 | `image/Image.h:188-221、251-286` | `read(void*,len)` 内存路径无 `IsSupportedImageFormat` 校验；`translate` 漏 pixSize==2（16bpp）分支且 `create()` 用 malloc → 返回未初始化内存 | LoadMemPic 坏图路径 |
| 13 | `image/ImageSearchAlgorithms.cpp:582、690` | FindColorEx/FindMultiColorEx 先拼串再判超限（off-by-one，上限 1800 实返 1801） | 轻微 |
| 14 | `opencv/TemplateMatcher.cpp:822-825` | 进程级 `static cv::Ptr<cv::ORB>` 无锁，多 COM 实例跨线程 detectAndCompute | 竞态（OpenCV ORB 有内部状态） |
| 15 | `capture/backends/WgcCapture.cpp` | `_width/_height` 收帧线程与 requestCapture 线程无同步读写（L1143 vs L546）；`ensureSharedResources` 删除重建 `_shmem` 仅持 `sharedResourceMutex_`，而 getFrameInfo 裸读无该锁（悬垂窗口） | Win11 默认 WGC 路径竞态 |
| 16 | `window/WindowService.cpp:67-100` 等 | `npid`/`enum_process_success_count` 无锁成员缓存，多线程同实例并发枚举互相清空 | 条件性（同实例并发才触发） |

### P2 可选（死代码/边界理论）

| # | 位置 | 问题 |
|---|---|---|
| 17 | `ocr/TesseractOcr.{h,cpp}` 整文件 | 死代码（仅 ImageSearchService.h 一行注释引用；l2_audit 已记录 alloc-nocheck 但未清），基于已移除的 tesseract 依赖 |
| 18 | `window/WindowState.cpp` 整文件 | 全库无外部调用（疑似未接线遗留），其中 288-296 还有 GetWindowLongA 循环不更新 i 的死循环缺陷 |
| 19 | `image/ImageView.h`、`opencv/ImagePreprocessor.h` | 空壳/零引用头文件 |
| 20 | `input/keyboard/WinKeyboard.cpp:8-40` | file-static `oem_code()` 死代码（连带整张扫描码表） |
| 21 | `image/ImageSearchAlgorithms.cpp:188,237,263` | `gen_next`/`Connectivity`/`extractConnectivity` 无调用者（Connectivity 内 create 在 empty 判前，空图会抛） |
| 22 | `algorithm/AStar.h:146-151` | `cell_count()` int 相乘溢出（>46340 尺寸）→ assign 巨大 size_t 抛 bad_alloc 跨 COM |
| 23 | `libop/com/op.rc:77-83` | 产品元数据 TODO 占位（CompanyName/FileDescription 等） |

### 已排除（子代理误报，人工复核确认无问题）

- `memory/ProcessMemory.cpp:694-758` FindData 句柄泄漏：**不存在**——所有 break 路径均汇聚到 L749 `CloseHandle`，提前 return 均在 OpenProcess 成功之前（opened==NULL）
- ipc/SharedMemory.h `at<T>()` void* 下标：模板未实例化，无运行时路径
- network/HttpClient WinHttpCrackUrl 空指针：MSDN 语义核实合法

---

## 二、功能增强候选

| # | 方向 | 现状 | 说明/成本 |
|---|---|---|---|
| E1 | **区域变化检测**（等待画面变化/稳定） | 无 | 基于现有 capture+区域哈希，等待"画面静止"再截图找图，游戏加载/切图场景高频需求；成本低 |
| E2 | **内存图输入找图**（FindPicMem 系） | MemoryImageSource 已有、Capture 有 GetScreenData | 把 `_src` 来源扩展为内存图/文件，脚本免绑窗口即可对图片找色找图；中成本 |
| E3 | AutoOcrEx 行序排序选项 | 现按 det 原始顺序 | 加 `--sort=lr/td`（左→右/上→下）输出稳定文本行序；低成本 |
| E4 | YOLO labels 便捷化 | `--labels=a,b,c` 需手写 | 支持 `--labels=@file.txt` 或内置 COCO 字典（ultralytics 默认 80 类）；低成本 |
| E5 | L2 暂缓项①：Capture 未绑定回退桌面截图 | 未绑定时区域调用空返回 | 改行为可能掩盖忘绑定——需拍板是否做 |
| E6 | L2 暂缓项②：去噪全局扩展 | 已按保守方案只进新方法 | 全局默认去噪会影响既有脚本找色语义——需拍板 |
| E7 | 真机验证清单 | 沙箱只能验编译 | D3D12/DXGI/WGC hook 真机冒烟、OCR/YOLO 实景对比、多显示器/DPI、resize 回归——**修复 P0-1/P0-2 后必须真机复测** |

---

## 三、建议落地顺序

1. **第一波（P0，5 项，纯代码修复，沙箱可编译验证）**：GdiCapture 共享段容量协商（镜像 HookCapture 的 ensureSharedFrameCapacity）、D3D12Capture 用 device->CreateCommandQueue 自建队列替代野指针取 queue、str2colordfs 空段防护、titles 自赋值改正、WinMouse/DxMouse 失败置 0。
2. **第二波（P1 中确定性高的 6/7/12/13）**：FindWindow 系 NULL 防护 + wcsstr 判空、GetWindowState 语义对齐、Image::read 格式校验+16bpp 分支、off-by-one。
3. **第三波（P1 竞态类 8/9/10/11/14/15）**：需谨慎改动的并发/钩子路径，逐个确认。
4. **第四波（P2 死代码清理 + E 功能）**：确认无引用后删/接线。
5. D3D12/WGC 修复项需真机验收，沙箱仅能保证编译与不回归。

## 四、待拍板

1. P0 五项是否全部开工？（预计 60-80 分钟含编译回归）
2. P1 第一波（6/7/12/13）是否一并做？
3. E1/E3/E4 三个低成本功能增强是否纳入本轮？
4. E5/E6（两个 L2 暂缓项）最终决定：做 / 不做？
