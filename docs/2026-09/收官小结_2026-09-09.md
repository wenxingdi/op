# OP 库评审修复收官小结

- 日期：2026-09-09
- 范围：`56f35d9`（L0/L1 修复起点）→ `967c413`（WgcCapture 真机复核收尾）
- 规模：**27 个提交 · 106 文件 · +6193 / -1078 行**
- 状态：A/B/C/D 真机验收闭环、P1 竞态/P2 死代码全清、仅 E 组挂起等模型

## 一、主线成果（按阶段）

| 阶段 | 提交 | 核心内容 | 验证 |
|---|---|---|---|
| L0 基座修复 | 56f35d9 | 构建/minhook 静态化/系统原语缺陷 | nmake + 单测 |
| L1 系统原语 | 8e208c4 | DllInjector + ProcessMemory 建议修 A/B/F | op_test |
| L2 核心子系统 | 8bec55e / 9f197e5 / b63fe74 | DisplayHook is_capture 竞争、dx 三通道开关、GDI 客户区偏移每帧重算 | op_test + 真机 |
| L3 COM 接口层 | 6ffae1b | Invoke try/catch + 13 方法 NULL 检查 + 41 方法 BSTR 所有权修复 | 编译 + 用例 |
| 优化轮 | 8e4bdca | CPU 采样基准/注入超时释放/SWIG 异常/构建环境 | op_test |
| OCR 引擎内置化 | d10c796 / 7de779d / 6fcd028 / 6ea3ff3 | PP-OCRv4 进程内 ONNX（编入 DLL 资源段）+ `--charset` 白名单 + AutoOcrLine 切行 | op_test 127 + ctypes 真 DLL 冒烟 |
| 第二轮核查落地 | 6314cbd / 70170dc | P0/P1 修复 + E3/E4 增强 + YOLO 引擎内置化（方案 B） | op_test |
| **A 组 GDI 放大** | f0d689d | refreshMetrics 放大后实时刷新帧尺寸 | 真机 PASS |
| **B 组 OCR 三件套** | 3c61c19 | autoocr_line 投影切行修整窗压扁错字 + 推理线程参数化 | 真机复核 PASS（错字消除） |
| **C 组 WGC 崩溃** | 026123a | Win10 18362 背靠背会话崩溃 SEH 兜底 + 进程熔断 | 隔离逐例 10/10 PASS |
| **D 组 D3D12 hook** | 95fc42e | 注入依赖 0xC0000135 治本——远端 SetDllDirectoryW | 自建载体真机闭环 ALL PASSED |
| P1 竞态批 1 | a35eb3e | ORB `static`→`thread_local`；UnBind 先远端释放后清引用 | nmake + 零回归 |
| P1 竞态批 2 | af037b2 | InputHook `g_stateMutex` 消无锁 RMW；dinput 不再谎报成功 | nmake + 零回归 |
| P1 竞态批 3 | 1be3057 | Pipe `CancelSynchronousIo` 防 join 死等；WgcCapture 锁化 + getFrameInfo 覆写 | nmake + 零回归 + **真机 10/10** |
| P2 死代码 | 1bf2c89 | 删 4 死文件 + GetTopWindowSp + oem_code + gen_next/Connectivity 系（**-403 行**） | nmake + 零回归 |
| L2 暂缓拍板 | 37fab58 | E5 自动回退桌面补 setlog；E6 去噪维持分界 | nmake + 零回归 |
| OCR 性能核查 | （纯核查） | 报告 `OCR_性能核查.md`：49ms 稳态真实、3 倍波动归因共存负载 | 沙箱复现闭环 |
| 收尾盘点 | 2c48143 | 根目录清理 + 基线修正 + doc2 md 入库 | — |
| 发布化 | 6dd9e57 | op-capi(ctypes) DLL 预置，`pip install ./bindings/python` 开箱可用 | 仓库外无参构造验证 |
| 收尾 docs | 7d1b56e / ea54241 / 967c413 | SWIG 保留备选 / CHANGELOG / 真机复核关闭 | — |

## 二、真机验收四组闭环

| 组 | 内容 | 结果 | 关键修复 |
|---|---|---|---|
| A | GDI/normal 放大超容 Capture | PASS | f0d689d 帧尺寸刷新 |
| B | OCR 三件套 + 耗时 | PASS | 3c61c19 切行修复；42.6ms 稳态达标（50ms 可接受定案） |
| C | WgcTest 真机窗口组 | **10/10 PASS** | 026123a SEH 兜底 + 进程隔离；崩因=18362 系统 bug 非库缺陷 |
| D | D3D12 hook 截图 | PASS | 95fc42e 注入依赖治本；渲染 hook 链路首次端到端跑通 |
| E | YOLO 自训模型 | **挂起** | 机制就绪（v8/v11 转置自适应 + `--labels=@file`），等模型到位三参重跑即验 |

## 三、竞态与死代码（第二轮核查遗留清零）

- **P1 六项三批闭环**：ORB 线程隔离 / UnBind 释放序 / InputHook 锁化 / dinput 谎报 / Pipe 死等 / Wgc 悬垂——每批 nmake + op_test 零回归，批 3 真机复核 10/10 无功能回归
- **P2 死代码清理**：-403 行；**修正报告 2 处误判**（#18 WindowState.cpp 8/9 方法在用只能删单方法；pyop 非死代码是 SWIG 通道）
- **核查关键结论**：GDI/Hook 后端 ensure 删重建全程持 `_pmutex` 无同病；悬垂仅 Wgc 独有已根治

## 四、测试基线（当前）

- op_test：**182 用例 · 146 PASS · 仅 1 个已知环境 FAIL（MouseKeyTest.WaitKey）** · WgcTest 沙箱 segfault 非回归（无 WGC worker）
- WGC 真机隔离：10/10 PASS（`run_wgc_isolated.bat`）

## 五、剩余挂起（均已入档）

1. **E 组 YOLO**：等任一合法 yolo 检测 onnx（v8/v11 各一更佳），到位后 `smoke_real.py --yolo-model/--yolo-image/--yolo-labels` 三参重跑即验，免改码
2. **push**：暂缓（白名单式网络封锁，本地记录为准：git 链 + CHANGELOG + memory 三层）
3. **遗留噪音 2 件**（降级无碍）：unbind OpUnBindWindow failed、SetDllDirectoryW RPC 取结果异常

## 六、发布格局（已定案）

| 通道 | 包名 | 形态 | 状态 |
|---|---|---|---|
| ctypes | op-capi（bindings/python） | 纯 .py + 包内 DLL | **现行**，pip 零依赖开箱可用 |
| SWIG | op-plugins（根 pyproject） | 编译型 + `_pyop.pyd` | **备选保留**，不退役 |

## 七、经验沉淀（防再犯）

- doc 内 hash 锚定用日期+数量（amend 致 hash 漂移实测 3 轮）
- 涉及目录删除类 git 操作后立即 git status 核对（git rm 曾致 libop 目录整体消失，checkout 恢复 + 编辑重放）
- 大批量 rm 被安全钩子 SIGTERM，分批 `rm -f` 正常
- 沙箱无 WGC worker / hook 类改动一律真机复核
- Git Bash export INCLUDE/LIB 必须 Windows 盘符格式（POSIX 路径 cl 不认）；PATH 跨调用不持久须同命令携带
